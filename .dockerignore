# Git
.git
.gitignore
.gitattributes

# Next.js build output + caches
.next
out
build
dist

# node_modules is rebuilt inside the deps stage
node_modules

# IDE / OS junk
.vscode
.idea
.DS_Store
Thumbs.db

# Dependency / build logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Local env files (docker-compose injects these instead)
.env
.env.local
.env.*.local
.env.sprint
.env.sprint.example

# Tests + test artifacts
coverage
playwright-report
test-results
.nyc_output

# Spec / internal docs (not needed for the runtime image)
.kiro
docs

# Docker-specific
docker-compose.yml
docker-compose.*.yml
Dockerfile
.dockerignore
